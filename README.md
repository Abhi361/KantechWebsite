# KantechSystems Website (Minimal Static)

This repo contains a minimal static site scaffold for KantechSystems.

Quick start

1. Open `index.html` in a browser for a local preview.
2. Or serve with a static server (recommended):

```bash
# using Python 3
python -m http.server 8000
# then open http://localhost:8000
```

